import React from 'react';
import MoviesList from '../MoviesList/MoviesList';

const Home = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export default Home;
